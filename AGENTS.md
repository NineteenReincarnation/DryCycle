# AGENTS.md

## 工程原则

- **优先使用正常代码边界**：DryCycle 自有代码优先直接修改，模块协作优先使用正常调用、接口、事件或明确扩展点；Hook、Detour、IL Patch 等侵入式手段用于确有必要的边界集成，不应成为自有模块之间的默认通信方式。
- **保持职责边界**：适配、兼容和 UI 层负责连接与呈现，不复制或重新实现其所连接模块的核心业务、AI、物理或持久化逻辑。
- **先修根因**：优先修复导致错误的实际原因，不用回滚、吞异常、重复补丁或降级路径掩盖本可直接修复的问题。
- **失败必须可观察**：不得静默吞掉失败；记录原始异常，并在执行回滚、清理或恢复时记录其结果，不得用后续异常覆盖最初的失败原因。
- **区分核心与可选功能**：可选功能可以隔离失败并安全降级；核心流程或不可逆操作失败时不得伪装成功，也不得留下被误认为有效的不一致状态。
- **保护作者数据**：生成缓存和运行时合并结果不得作为作者数据写入目标；持久化失败不得破坏最后一份有效的作者数据。
- **集中持久化职责**：UI 和其他表现层只表达保存意图，不各自实现文件持久化规则；实际写入由明确的持久化边界统一负责。
- **保护未保存修改**：界面、Session、Region 或运行时对象的关闭、切换、重建不得静默丢弃未保存作者数据；无法安全迁移时应保留、阻止操作或明确报告冲突。
- **依赖必须透明**：新增运行时依赖不得隐式引入；需要额外程序集、原生库、资源或安装步骤时必须明确部署要求，并避免无必要地增加部署复杂度。
- **优先真实验证**：能够通过编译、启动或运行测试直接验证的问题，不用字符串检查代替；静态 Guard 不能作为真实运行正确性的证明。
- **Guard 只守硬不变量**：只有能够客观、稳定、低误报地静态验证，并且不依赖当前私有实现细节的长期约束才进入 Guard；设计原则写入 AGENTS，运行行为通过测试验证。
