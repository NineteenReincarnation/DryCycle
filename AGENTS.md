# AGENTS.md

## 工程原则

- **优先改源码**：DryCycle 自己拥有的代码，优先直接修改原实现；模块协作使用正常调用、接口、事件或明确扩展点。
- **避免内部 Hook**：不要为了省事对 DryCycle 自身方法层层 Hook、Detour、IL Patch 或反射劫持。确有必要时，应有明确的技术原因。
- **外部代码才优先 Hook**：Rain World 本体、第三方 Mod 等不可直接控制的代码，可以使用 On/IL/Detour 等合适方式集成。
- **保持单一职责**：Integration/Compatibility 层负责连接外部系统，不应逐渐演变成第二套业务、AI 或物理实现。
- **先修根因**：不要用回滚、吞异常或额外补丁掩盖可直接修复的问题。需要回滚时，必须记录原始错误以及清理是否成功。
- **启动失败可观察**：启动、回滚和清理失败不得静默吞掉；保留原始异常和清理结果。
- **区分核心与可选功能**：可选工具可以隔离失败；核心或不可逆状态不得为了继续启动而伪装成功。
- **Guard 只守硬不变量**：需要理解设计意图和上下文的工程判断写在这里；可客观验证的长期约束才进入 Guard。
